general:
  artifacts:
    - bundle.js

machine:
  node:
    version: 6.0.0

deployment:
  github:
    branch: master
    commands:
      - git config --global user.email a.tokai.wain@gmail.com
      - git config --global user.name Naoki Iwata
      - git add -f _build
      - git commit -m "build [ci skip]" || true
      - git push git@github.com:staffwars/staffwars.git -f :gh-pages || true
      - git push git@github.com:staffwars/staffwars.git master:gh-pages

